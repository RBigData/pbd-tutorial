\subsection{Summary}
\makesubcontentsslidessec


\begin{frame}
  \begin{block}{Summary}
    \begin{itemize}
      \item \emph{Profile, profile, profile.}
      \item Use \code{system.time()} to get a general sense of a method.
      \item Use \pkg{rbenchmark}'s \code{benchmark()} to compare 2 methods.
      \item Use \code{Rprof()} for more detailed profiling.
      \item Other tools exist for more hardcore applications (\pkg{pbdPAPI} and 
\pkg{pbdPROF}).
    \end{itemize}
  \end{block}
\end{frame}